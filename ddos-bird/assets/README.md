# Assets

В этой папке можно заменить модели игры:

- `background.svg` - фон игры.
- `bird.svg` - персонаж.
- `pipe-red.svg` - столб до пролета.
- `pipe-green.svg` - столб после пролета (когда начисляются очки).

Можно использовать свои PNG/SVG/JPG и просто поменять пути в `script.js`.
